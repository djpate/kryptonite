{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Phase 7.5 Gate — Repo Conventions Preflight (base)",
  "description": "Base gate for Phase 7.5. Phase 7.5 was added in kryptonite 0.7.0; pre-0.7.0 epics skip this phase entirely. The conventions-population requirement is in 07_5.0.7.0.json so version-gating works the same as other phases.",
  "type": "object",
  "required": ["epic", "state", "repos"],
  "properties": {
    "epic": {
      "type": "object",
      "required": ["current_phase"],
      "properties": {
        "current_phase": { "type": "number", "minimum": 7 }
      }
    },
    "state": {
      "type": "object",
      "required": ["stories"],
      "properties": {
        "stories": { "type": "array", "minItems": 1 }
      }
    },
    "repos": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "path"]
      }
    }
  }
}
